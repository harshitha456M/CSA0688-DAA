#include<stdio.h>
int main()
{
	int arr[20],i,n,max;
	printf("enter no of elements:");
	scanf("%d",&n);
	printf("elements:");
	for(i=0;i<n;i++)
	{
		scanf(" %d",&arr[i]);
		
	}
	max=arr[0];
	for(i=0;i<n;i++)
	{
		if(arr[i]>max)
		{
			max=arr[i];
		}
}
printf("largest:%d",max);
}
