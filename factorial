#include<stdio.h>
int main()
{
	int i,n,fact;
	printf("enter the number:");
	scanf("%d",&n);
	if(n<0)
	{
		printf("invalid");
	}
	else if(n==0)
	{
		printf("factorial of 0 is 1");
	}
	else
	{
		for(i=1;i<=n;i++)
		{
			fact=fact*i;
		}
		printf("factorial of %d is %d",n,fact);
	}

}
